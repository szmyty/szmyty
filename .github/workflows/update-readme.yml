name: Update README
on:
  schedule:
    # Run daily at 00:15 UTC
    - cron: "15 0 * * *"
  workflow_dispatch:

jobs:
  build:
    name: Update this repo's README with recent activity
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      # Cache GitHub API responses to prevent rate limiting
      - name: Cache GitHub API responses
        uses: actions/cache@v4
        with:
          path: ~/.cache/github-activity
          key: github-activity-${{ steps.date.outputs.date }}
          restore-keys: |
            github-activity-

      - uses: jamesgeorge007/github-activity-readme@v0.5.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
