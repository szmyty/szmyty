{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://egohygiene.io/schemas/practices/reflection.schema.json",
  "title": "Reflection",
  "description": "Canonical local-first reflection record independent of Flutter implementation details.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "body",
    "tags",
    "createdAt",
    "updatedAt"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable reflection record identifier."
    },
    "title": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 1,
      "description": "Optional short label for the reflection."
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Primary reflection content capturing conscious awareness of experience."
    },
    "tags": {
      "type": "array",
      "description": "Optional user-defined tags that support review and future insight generation.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "default": []
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "When the reflection record was first created."
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "When the reflection record was last modified."
    }
  }
}
