name: Medium RSS Sync

on:
  workflow_dispatch:
  schedule:
    - cron: "30 8 * * *"
  push:
    branches:
      - main
    paths:
      - "publishing/tools/medium-rss/**"
      - "publishing/channels/medium/config.yaml"
      - ".github/workflows/medium-rss-sync.yml"

concurrency:
  group: medium-rss-sync
  cancel-in-progress: false

jobs:
  sync:
    name: Sync Medium Feed
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: pipx install poetry

      - name: Cache Poetry virtualenv
        uses: actions/cache@v4
        with:
          path: publishing/tools/medium-rss/.venv
          key: ${{ runner.os }}-poetry-medium-rss-${{ hashFiles('publishing/tools/medium-rss/poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-poetry-medium-rss-

      - name: Install dependencies
        working-directory: publishing/tools/medium-rss
        run: |
          poetry config virtualenvs.in-project true
          poetry install --no-interaction

      - name: Run Medium RSS sync
        working-directory: ${{ github.workspace }}
        run: poetry run --directory publishing/tools/medium-rss medium-rss sync --config publishing/channels/medium/config.yaml

      - name: Commit synchronized content
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add publishing/channels/medium/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "docs(medium): 📝 sync Medium article archive [skip ci]"
            git push
          fi
