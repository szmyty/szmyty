# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
# This file configures the Dart analyzer for strict, bulletproof static analysis.
# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  exclude:
    - "build/**"
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.factory.dart"

linter:
  rules:
    # Disable the 80-character strict limit so our 120-width formatter takes precedence
    lines_longer_than_80_chars: false

    # Explicitly enforce trailing commas everywhere to lock down style conformity
    require_trailing_commas: true

    # Enforce clear public interface patterns
    public_member_api_docs: false
    avoid_print: true
    prefer_single_quotes: true
    avoid_catches_without_on_clauses: false
    avoid_equals_and_hash_code_on_mutable_classes: false
    avoid_field_initializers_in_const_classes: false
    avoid_positional_boolean_parameters: false
    avoid_returning_this: false
    cascade_invocations: false
    comment_references: false
    deprecated_consistency: false
    directives_ordering: false
    discarded_futures: false
    document_ignores: false
    join_return_with_assignment: false
    missing_code_block_language_in_doc_comment: false
    no_adjacent_strings_in_list: false
    no_default_cases: false
    no_runtimetype_tostring: false
    one_member_abstracts: false
    prefer_asserts_with_message: false
    prefer_foreach: false
    sort_pub_dependencies: false
    unawaited_futures: false
    use_setters_to_change_properties: false

formatter:
  page_width: 120
  trailing_commas: preserve
