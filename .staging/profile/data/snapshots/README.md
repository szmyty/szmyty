# Historical Data Snapshots

This directory stores historical snapshots for trend analysis.

See the full documentation in the main README or run:
```bash
python scripts/store-historical-snapshot.py --help
```
