# Java JetBrains profile

Provides a conservative Checkstyle IDEA baseline using the rule sets bundled
with the plugin. A consuming repository should select and activate the rule
set that matches its canonical command-line Checkstyle configuration.

Formatter and linter policy remain owned by the repository quality platform;
this profile only improves IDE integration.
