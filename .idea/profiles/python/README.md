# Python JetBrains profile

Configures the Ruff plugin to use the repository's canonical
`pyproject.toml`. The Ruff executable and JetBrains plugin are workstation
concerns and should be provisioned through `realm`.
