# Flutter JetBrains profile

Provides a shared run configuration for a conventional Flutter application
whose entry point is `lib/main.dart`.

Repositories with another entry point should copy and adapt the configuration
rather than changing the reusable profile globally.
